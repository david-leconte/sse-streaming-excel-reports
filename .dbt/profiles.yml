default:
  target: app
  outputs:
    app:
      type: duckdb
      path: "ducklake:sqlite:data/warehouse/warehouse.sqlite"
      extensions:
        - ducklake
        - sqlite
      options:
        data_path: "data/warehouse/data_files"
      
