default:
  target: app
  outputs:
    app:
      type: duckdb
      path: "ducklake:sqlite:{{ user_project_dir }}/data/warehouse/warehouse.sqlite"
      extensions:
        - ducklake
        - sqlite
      options:
        data_path: "{{ user_project_dir }}/data/warehouse/data_files"
