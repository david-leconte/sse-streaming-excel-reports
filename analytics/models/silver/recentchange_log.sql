SELECT
	id,
	namespace,
	title,
	comment,
	user,
	bot,
	minor,
	uri,
	domain,
	dt,
	log_type,
	log_action,
	log_params.img_timestamp,
	log_params.duration,
	log_params.flags,
	log_params.sitewide,
	log_params.target,
	log_params.noredir,
	log_params.ofield,
	log_params.nfield,
	log_params.olduser,
	log_params.newuser,
	log_params.movepages,
	log_params.suppressredirects,
	log_params.edits,
	log_params.derived,
	log_params.interwiki,
	log_params.oldtitle,
	log_params.count.files,
	log_params.description,
	log_params.cascade,
	log_params.expiry,
	log_params.dest,
	log_params.mergepoint,
FROM
	{{ ref('recentchange') }}
WHERE
	"type" = 'log'